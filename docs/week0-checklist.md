# Week 0 checklist — moved

This checklist is absorbed into the field manual. The sessions, every done-when, the
[NICK]/[FLEET] split, and the gate now live at:

**`docs/manual/week-00.html`**

The front door is `docs/manual/index.html`; the next action lives in
`docs/tomorrow-note.md`.
