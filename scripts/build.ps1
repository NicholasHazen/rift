# Placeholder. Replaced during week 0 [AGENT] work once the UE project exists
# (real UnrealBuildTool invocation gets captured here and in CLAUDE.md Build section).
# Exists because week 7 packages a Windows client and a Windows box may join the loop.
# ASCII-only on purpose: BOM-less non-ASCII garbles under Windows PowerShell 5.1.
[Console]::Error.WriteLine("FATAL: UE project not created yet -- see docs/week0-checklist.md")
exit 1
