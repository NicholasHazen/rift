#!/usr/bin/env bash
# Placeholder. Replaced during week 0 [AGENT] work once the UE project exists
# (real UnrealBuildTool invocation gets captured here and in CLAUDE.md §Build).
set -euo pipefail
echo "FATAL: UE project not created yet — see docs/week0-checklist.md" >&2
exit 1
